# 工作区记忆

## 工作区定位

- `project` 是主工作区与主框架
- `formal-writing` 是第一个正式吸收的能力模块
- 后续模块应继续吸收到 `project` 中，而不是复制出新的平行主工作区

## 结构原则

- `claude/` 是能力与长期记忆的统一来源
- `claude/` 与 `codex/` 是各自的适配层
- `output/` 是统一交付目录
- `tmp/` 不是长期知识来源

## 记忆原则

- 长期知识优先进入 `claude/memory/`
- 运行时 memory MCP 是补充层，不是唯一记忆层
